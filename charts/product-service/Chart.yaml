apiVersion: v2
name: product-service
description: FastAPI product catalogue - products, inventory, reviews and orders
type: application

# Chart version: bump when the templates or values change.
version: 0.2.0

# Application version: the image tag this chart ships by default.
appVersion: "0.2.0"
